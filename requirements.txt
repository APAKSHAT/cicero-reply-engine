anthropic>=1.2.0
openai>=3.0.0        # only for the OpenRouter backend (src/cicero/llm.py)
pydantic>=2.0
PyYAML>=6.0

# Only needed to run against a real inbox (src/cicero/adapters/google.py).
# The demo runs entirely on the mock adapters without these.
# google-api-python-client>=2.100.0
# google-auth-oauthlib>=1.2.0
